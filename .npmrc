; Required only by eslint-plugin-react@7.37.5, whose peer range stops at
; ESLint 9.7 while the project runs ESLint 10. Every other dependency
; resolves cleanly — retry without this once the plugin ships ESLint 10 support.
legacy-peer-deps=true
